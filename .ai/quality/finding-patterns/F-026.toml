version = 1

[finding]
source = "issue #67 adversarial review"
defect_class = "manual-risk-upgrade-does-not-upgrade-gates"
severity = "Defect"
affected = "scripts/quality/pr_ready.py"
root_cause = "readiness computed required gates from the path classifier before reading a higher author-declared risk class"
why_tests_missed = "existing tests covered understatement and already-R3 paths, but not a semantic upgrade above a lower path minimum"
regression = "test_a_manual_r3_upgrade_enforces_the_cumulative_r3_gates"
generalized = "an effective safety level must be the maximum of every authoritative minimum and explicit conservative upgrade"
workflow_change = "readiness now derives cumulative gates from max(classified risk, declared risk)"
