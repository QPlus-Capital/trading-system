version = 1

[finding]
source = "PR #123 complete independent re-review"
defect_class = "fixed-width-identity-witness-accepts-short-zero-padded-input"
severity = "Note"
affected = "live/accounts.py"
root_cause = "formatting a short integer to four digits made a login shorter than the code-owned suffix appear to match the suffix"
why_tests_missed = "suffix tests used only realistic longer logins and never supplied a value shorter than or equal to the witness itself"
regression = "test_login_must_be_longer_than_its_code_owned_suffix"
generalized = "a suffix identity witness must first prove the raw identifier contains an independent prefix; formatting or padding cannot create that evidence"
workflow_change = "the raw login must be longer than the suffix and end with it before integer conversion is accepted"
