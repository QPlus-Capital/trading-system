version = 1

[finding]
source = "issue #125 Codex independent review 125-R1"
defect_class = "quality-policy-content-omitted-from-baseline-identity"
severity = "P1"
affected = "scripts/quality/mutation.py, .ai/quality/mutation-baseline.toml"
root_cause = "mutation reports and the committed baseline identified targets only by ID while the function-selecting mutant_patterns remained unbound"
why_tests_missed = "the differential oracle varied counts, statuses, survivor names, scopes, and target IDs but could not represent a same-ID and same-path pattern substitution"
regression = "test_policy_substitution_fails_with_exact_survivors_and_an_improved_score; test_policy_fingerprint_ignores_only_target_and_pattern_order; test_fast_report_fingerprints_the_whole_policy"
generalized = "a quality baseline must bind every policy field that determines what is measured, not only display identifiers or aggregate outcomes"
workflow_change = "mutation reports and baselines require a canonical complete-target fingerprint, and readiness fails on a missing or changed identity"
