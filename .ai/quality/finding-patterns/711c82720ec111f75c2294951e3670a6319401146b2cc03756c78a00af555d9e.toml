version = 1

[finding]
source = "PR #143 review-remediation gate run"
defect_class = "migration-guard-forbids-supported-additive-registry-growth"
severity = "Defect"
affected = "tests/test_finding_registry_split.py"
root_cause = "the historical 58-entry severity-migration assertion compared its snapshot against the entire live registry, so every valid content-addressed addition failed the suite"
why_tests_missed = "the registry split tests proved two-branch merges in isolation but the historical migration guard had never run with a post-migration finding file present"
regression = "test_severity_migration_preserves_every_pattern_and_rejects_content_duplicates"
generalized = "a migration invariant must bind the migrated subset rather than freeze an extensible registry at its migration-time cardinality"
workflow_change = "the guard still requires all 58 migrated digests and severities exactly while permitting independently validated content-addressed additions"
