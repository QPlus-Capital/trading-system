version = 1

[finding]
source = "PR #59 (constitution + risk model)"
defect_class = "unenforced-stated-rule"
severity = "P2"
affected = "docs/engineering/constitution.md, tests/test_import_boundaries.py"
root_cause = "the constitution stated a dependency boundary (nothing imports monitoring; governance docs are R3) that no test actually scanned for"
why_tests_missed = "the rule lived only in prose; the test suite did not cover the package it named"
regression = "test_nothing_imports_the_monitoring_layer; governance-doc classification cases"
generalized = "every stated invariant needs an executable check over the ACTUAL surface it governs, or it is decoration"
workflow_change = "risk model safe-by-default; import-boundary tests scan every package"
