version = 1

[finding]
source = "PR #70 (mutation result artifact)"
defect_class = "hidden-artifact-default-exclusion"
severity = "P2"
affected = ".github/workflows/mutation.yml"
root_cause = "the workflow wrote its machine report below `.ai/`, but actions/upload-artifact excludes hidden paths unless explicitly enabled"
why_tests_missed = "the upload step had never reached a completed report and no test bound the action's hidden-file option"
regression = "test_mutation_workflow_uploads_the_hidden_machine_report"
generalized = "for dot-directory build evidence, assert both file creation and the uploader's hidden-file inclusion setting"
workflow_change = "upload the machine-readable mutation report with include-hidden-files enabled"
