version = 1

[finding]
source = "PR #123 independent live-money review"
defect_class = "security-scan-passes-vacuously-or-misses-canonical-form"
severity = "P2"
affected = "tests/test_live_accounts.py"
root_cause = "the anti-recommit regex described the removed source assignment instead of the new environment syntax, excluded tests, and ran git ls-files from a nested mutant copy where it enumerated zero relevant files"
why_tests_missed = "the test asserted only an empty finding list and never asserted the scanned path set, count, canonical leak forms, or nested-worktree behavior"
regression = "test_login_literal_guard_matches_plausible_reintroduction_forms; test_tracked_text_includes_tests_and_is_non_vacuous; test_tracked_text_resolves_the_git_root_from_a_nested_copy"
generalized = "a repository security scan must prove its input population and test the exact representation introduced by the change before an empty finding set can count as evidence"
workflow_change = "the scan resolves the Git top-level, includes tests, fails on an implausible tree, and separately checks bare long numbers in operator documentation"
