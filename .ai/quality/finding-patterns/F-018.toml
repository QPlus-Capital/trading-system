version = 1

[finding]
source = "issue #66 (Claude command hooks)"
defect_class = "command-spelling-matcher-gap"
severity = "P1"
affected = "scripts/quality/hooks/decisions.py"
root_cause = "the first live-execute regular expression put a word boundary before the non-word --mode token, so the documented dotted-module command was allowed"
why_tests_missed = "the implementation had not yet executed the exact command spelling from the acceptance criterion"
regression = "test_dangerous_command_decision_blocks_unsafe_and_allows_safe"
generalized = "security command matchers must execute the exact documented unsafe spellings plus benign near-neighbours"
workflow_change = "each hook decision has paired block/allow tests using production command forms"
