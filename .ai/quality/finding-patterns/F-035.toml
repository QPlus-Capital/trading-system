version = 1

[finding]
source = "issue #60 constitution audit"
defect_class = "canonical-component-overwritten-by-derived-value"
severity = "Defect"
affected = "research/portfolio/stats.py"
root_cause = "the broker-aware helper stored gross R and then overwrote that column with gross plus swap instead of preserving the cost decomposition"
why_tests_missed = "tests covered R recovery and swap pricing separately but never invoked the helper's broker-aware branch and asserted all three column identities"
regression = "test_market_trades_preserves_gross_and_separate_swap; test_market_trades_records_zero_swap_when_broker_has_no_market_spec"
generalized = "when a derived financial quantity combines canonical components, preserve each component and assert the algebraic identity at the real orchestration boundary"
workflow_change = "broker-aware return helpers now require behavioural gross/separate-cost/net schema guards"
