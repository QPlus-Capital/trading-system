version = 1

[finding]
source = "PR #130 first ready-state Linux run review"
defect_class = "platform-proxy-skip-does-not-bind-the-required-tool"
severity = "P1"
affected = "tests/test_quality_mutation.py, justfile"
root_cause = "the real Mutmut probe skipped by operating system even though its actual precondition is the separately supplied Mutmut console script"
why_tests_missed = "the old Windows-only test job always skipped the node, while the first Linux test job satisfied the platform guard without installing the ad hoc mutation dependency"
regression = "test_a_real_weakened_test_increases_survivors_and_is_caught under full-quality without Mutmut and mutation-self-test with mutmut==3.5.0"
generalized = "a capability-dependent test must skip on the capability it consumes, not on a platform used as an unreliable proxy"
workflow_change = "the test reuses mutation_executable for availability, skips cleanly without the tool, and executes in the mutation job that supplies it"
