version = 1

[finding]
source = "issue #99 real-artifact direction reconciliation"
defect_class = "stable-wrong-categorical-value-hidden-by-drift-gates"
severity = "Defect"
affected = "research/portfolio/trades.py"
root_cause = "closed-position state side=FLAT was mistaken for opening direction even though the independent entry-side field carries BUY or SELL"
why_tests_missed = "synthetic reports invented side=LONG/SHORT and byte-identity checks only proved the all-false artifact was reproducible"
regression = "test_closed_position_uses_entry_side_for_direction; test_unrecognized_entry_side_fails_closed; test_extracted_entry_side_drives_long_low_and_short_high; real per-market BUY/SELL reconciliation"
generalized = "a stable wrong value is invisible to drift gates: categorical correctness must be asserted against an independent authoritative source, not a previous artifact"
workflow_change = "producer tests mirror the real closed-record schema and high-risk categorical artifacts require independent distribution/source reconciliation"
