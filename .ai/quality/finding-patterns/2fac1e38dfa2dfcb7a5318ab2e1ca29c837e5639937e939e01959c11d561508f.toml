version = 1

[finding]
source = "PR #140 review 4818275329 findings S1 and S3"
defect_class = "guard-boundaries-and-entrypoint-preconditions-covered-on-only-one-side"
severity = "Suspected defect"
affected = "scripts/quality/board.py, tests/test_quality_board.py"
root_cause = "tests covered multiple risk labels but not zero labels, and exercised a valid approval sequence without proving that the public arm entrypoint still rejects malformed bodies and unknown risk classes before writes"
why_tests_missed = "happy-path ordering and one conflict case were mistaken for complete coverage of cardinality and validation boundaries"
regression = "test_start_refuses_a_card_without_any_risk_label; test_start_guard_condition_set_is_unchanged; test_arm_refuses_unapprovable_input_before_any_write"
generalized = "cardinality guards require zero, one, and many cases, and a public workflow entrypoint must test each precondition through that entrypoint with an assertion that no external write occurred"
workflow_change = "the start predicate is exhaustively compared over status and label combinations, and arm rejects invalid bodies and risk classes before any gateway mutation"
