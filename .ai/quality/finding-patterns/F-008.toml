version = 1

[finding]
source = "PR #59 (constitution + risk model)"
defect_class = "false-confidence-guard"
severity = "P2"
affected = "tests/test_import_boundaries.py"
root_cause = "a guard asserted a token was PRESENT in source text (f'{engine}(' in src), which a comment, docstring, or annotation satisfies -- it did not prove the behaviour"
why_tests_missed = "the guard was the test; nothing checked the guard actually binds"
regression = "test_both_execution_adapters_construct_the_shared_signal_engine now parses ast.Call"
generalized = "a guard must assert BEHAVIOUR via the AST or a real run, never a substring of the source; and every new guard is proven red before it is trusted"
workflow_change = "constitution section 8 (behavioural assertions); adversarial-review test-for-false-confidence step"
