version = 1

[finding]
source = "PR #59 (constitution + risk model)"
defect_class = "doc-model-drift"
severity = "P2"
affected = "docs/engineering/risk-classes.md, docs/architecture.md"
root_cause = "prose duplicated a list held authoritatively in the model (and in architecture.md), then drifted from it"
why_tests_missed = "agreement was checked by keyword presence, not structurally"
regression = "test_risk_doc_and_model_agree (no stale globs, defers to the model); test_architecture_doc_lists_both_allowlisted_crossings"
generalized = "do not duplicate a machine-authoritative list in prose; point to the source, or compare structurally in a test"
workflow_change = "risk-classes.md points to the model as authoritative"
