version = 1

[finding]
source = "issue #58 constant-basis audit"
defect_class = "coupled-accounting-model-not-updated-at-every-consumer"
severity = "Defect"
affected = "research/engine/continuous.py"
root_cause = "Stage 1 changed sizing and returns to a constant basis but retained a compounding running-equity viability check"
why_tests_missed = "the constant-basis test varied prior profits only, while separate tests ratified the obsolete ruin behavior instead of crossing the negative basis boundary"
regression = "test_fixed_basis_scores_after_cumulative_losses_exceed_the_basis; test_prior_loss_depth_never_changes_a_later_fixed_basis_score"
generalized = "when an accounting basis changes, enumerate sizing, denominators, path statistics, boundary guards, error rows, and downstream completeness as one coupled quantity"
workflow_change = "constant-basis scoring tests now cross both exact and beyond-basis cumulative losses and require later evidence to remain scored"
