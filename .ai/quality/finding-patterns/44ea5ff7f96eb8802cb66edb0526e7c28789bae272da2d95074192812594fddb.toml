version = 1

[finding]
source = "PR #140 review 4818275329 finding S2"
defect_class = "write-verification-defences-cannot-be-exercised-by-the-test-double"
severity = "Suspected defect"
affected = "scripts/quality/board.py, tests/test_quality_board.py"
root_cause = "the fake gateway could model sticky writes but could not interleave a third-party state change between a write and its verification read"
why_tests_missed = "concurrency branches were described in impact prose while every fake write deterministically changed only its own field"
regression = "test_move_refuses_when_status_changes_during_permit_withdrawal; test_move_refuses_when_a_third_party_readds_permit_after_status_write; test_withdraw_refuses_when_status_changes_during_permit_removal"
generalized = "a fail-closed read-after-write guard needs a one-shot test-double hook that can mutate external state at the write boundary; prose about concurrency is not executable evidence"
workflow_change = "FakeGateway has a one-shot on_write hook and every concurrent-modification refusal is reached by a behavioural test"
