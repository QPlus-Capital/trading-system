version = 1

[finding]
source = "continuous walk-forward"
defect_class = "unclassified-outcome-bucket"
severity = "Defect"
affected = "research/engine/continuous.py (window_returns)"
root_cause = "a position closing in a gap between windows (step > test) belonged to no result bucket while its PnL still moved the next window's balance"
why_tests_missed = "the study's windows are contiguous, so the gap case never arose in tests"
regression = "gapped-window attribution tests in test_research_continuous_windows.py"
generalized = "enumerate every domain outcome; assert each accepted record lands in exactly one bucket and none is dropped or double-counted"
workflow_change = "constitution section 8 (reconciliation properties)"
