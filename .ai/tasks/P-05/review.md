# Adversarial review

Pending implementation. The final review must attempt at least: correlated null candidates,
negative high-variance contamination, a scale transformation, zero variance, non-finite input,
metadata/CSV count mismatch, stale SPA bytes, a failed sensitivity hidden by a passing selected
length, forced-selection labeling, and a helper that is correct but never runs on the Stage-2 path.

## Findings

Pending implementation and adversarial review.

## Dispositions

Pending implementation and adversarial review.
